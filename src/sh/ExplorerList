#! /usr/bin/env bash

gsed 's#^.*$#-o -regex &/[^/]*#' |
    xargs bash -c 'find . -mindepth 1 \( -regex "./[^/]*" $* \) -print -o -prune' -- | sort |
    gawk -v n="$1" '$0=="./"n { ind=i+1 } { print substr($0,3); i++ } END { print ind ":" i }'
